spring:
  application:
    name: tagnote-web
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.xml
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/tagnote-project-db
    username: postgres
    password: postgres